# Gesu Assets

Structure:
- `fonts/`: Place `LexendDeca-VariableFont_wght.ttf` or `.woff2` files here.
- `icons/`: SVGs and icon assets.
- `images/`: Raster images/dashboards.

Usage:
Fonts are wired in `index.css` and `tailwind.config.js`.
