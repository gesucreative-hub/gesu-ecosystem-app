{
  "name": "gesu-ecosystem-v2",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev": "pnpm --filter gesu-shell dev",
    "dev:desktop": "pnpm --filter gesu-shell dev:electron",
    "dev:desktop:watch": "pnpm --filter gesu-shell dev:watch",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/chokidar": "^2.1.7",
    "concurrently": "^8.2.2",
    "electron": "^28.0.0",
    "eslint": "^8.56.0",
    "nodemon": "^3.0.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.0",
    "wait-on": "^7.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "firebase": "^12.7.0"
  }
}